<UserControl x:Class="Restaurant.UI.View.OrderHistory.OrderHistoryListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Restaurant.UI.View.OrderHistory"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ListView ItemsSource="{Binding OrderInfos}"
                  SelectedIndex="{Binding SelectedOrder, Mode=TwoWay}"
                  >
            <!-- Column Binding matches track object properties -->
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Id zamowienia" Width="150" DisplayMemberBinding="{Binding OrderId}" />
                    <GridViewColumn Header="Data zamowienia" Width="350" DisplayMemberBinding="{Binding OrderDate}" />
                    <GridViewColumn Header="Ilosc produktow" Width="250" DisplayMemberBinding="{Binding Quantity}" />
                    <GridViewColumn Header="Kwota" Width="200" DisplayMemberBinding="{Binding AmountTotal}" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
